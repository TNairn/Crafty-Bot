{
  "name": "crafty-bot",
  "version": "1.0.0",
  "description": "Searches the history of a text channel and removes new messages that match old ones.",
  "main": "bot.js",
  "type": "module",
    "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Taylor",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.1"
  }
}
